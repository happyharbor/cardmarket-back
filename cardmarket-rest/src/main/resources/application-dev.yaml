spring:
  datasource:
    username: postgres
    password: postgres

security:
  jwt:
    token:
      expire-length: 180000
  allowed-origins: http://localhost:3000

schedule:
  update-prices: 0 0 0 0 0 0